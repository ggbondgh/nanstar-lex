{
  "name": "nanstar-lex",
  "version": "0.1.0",
  "private": true,
  "description": "NanStar Lex Android wrapper for personal English practice.",
  "scripts": {
    "build": "node tools/build-capacitor-web.js",
    "cap:sync": "npm run build && npx cap sync android",
    "android:debug": "npm run build && npx cap sync android && node tools/build-android-debug.js",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/app": "^7.0.0",
    "@capacitor/browser": "^7.0.0",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
